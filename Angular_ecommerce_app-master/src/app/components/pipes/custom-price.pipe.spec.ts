import { CustomPipePrice } from './custom-price.pipe';

describe('CustomPipePrice', () => {
  it('create an instance', () => {
    const pipe = new CustomPipePrice();
    expect(pipe).toBeTruthy();
  });
});
